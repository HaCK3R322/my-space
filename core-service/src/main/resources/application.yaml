server:
  port: ${PORT:8080}

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:tasks-db}
    username: ${DB_USER:postgres}
    password: ${DB_PASS:local_password}

security:
  jwt:
    secret: ${JWT_SECRET:a_very_long_local_development_secret_key_12345}
